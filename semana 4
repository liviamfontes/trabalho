Código com estrutura de repetição gerado:
# Laço for para repetir uma ação
for i in range(1, 6):
    # Imprime o número atual
    print(i)


Código com estrutura de tomada de decisão:
# Pede ao usuário para digitar um número
numero = float(input("Digite um número: "))

# Estrutura de decisão para determinar se o número é positivo, negativo ou zero
if numero > 0:
    print("O número é positivo.")
elif numero < 0:
    print("O número é negativo.")
else:
    print("O número é zero.")
